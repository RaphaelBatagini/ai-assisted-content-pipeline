{
  "name": "site-template",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "node scripts/generate-sitemap.js",
    "start": "next start",
    "export": "next build",
    "dev:mock": "SITE_DATA_PATH=$PWD/mock-data-2 next dev",
    "build:mock": "SITE_DATA_PATH=$PWD/mock-data SITE_BASE_URL=http://localhost:3003 next build && SITE_DATA_PATH=$PWD/mock-data SITE_BASE_URL=http://localhost:3003 node scripts/generate-sitemap.js",
    "serve:mock": "npm run build:mock && npx serve out -p 3003"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
