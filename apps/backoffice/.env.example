# API URL — point to your local or remote API instance
NEXT_PUBLIC_API_URL=http://localhost:3001

# Stripe publishable key (optional — only needed if you init Stripe.js on frontend)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
